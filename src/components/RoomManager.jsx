import React, { Component } from 'react';

class RoomManager extends Component{
  constructor(props) {
    super(props);
    this.state = {
    };
  }
  render(){
    return (
      <div>
  ROOM MANAGER
      </div>
    );
  }
}

export default RoomManager;
